# End-to-end tests

Real CLI invocations on fixture datasets: inspect → eval → bench → report,
plus executing exported pipeline scripts and asserting they match `apply`.
Fully offline. Land from v0.1.
