# Golden (snapshot) tests

Snapshot tests (syrupy) for michi's outputs: terminal renderings, HTML
reports (timestamps and versions masked), emitted recipes, and exported
pipeline code. The main regression net for a tool whose product is its
output. Land from v0.1.
